;; The matcher is a `.kotoba` module and its tests run on nbb; this file exists
;; so `pattern.compile` -- portable .cljc, zero third-party deps -- has a
;; coordinate a consumer can name.
{:paths ["src"]
 :deps {}}
